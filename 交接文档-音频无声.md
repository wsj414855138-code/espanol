# 交接文档：浏览器"听不到声音"排查与修复（2026-08-15）

> 给 Codex / 任何接手此问题的 AI 与人。先读 [docs/progress.md](docs/progress.md) 了解项目背景，
> 再读本文。当前结论：**根因已定位并修复（服务端媒体响应头不兼容 Safari），另有配套日志与校验防回归。**
> 如果用户硬刷新后仍无声，按文末"若仍失败"清单继续。

## 一、现象

- 用户（Mac 桌面浏览器，localhost:8000/app/）点击任何播放点（词汇发音/例句/听辨/跟读常速慢速/听写）
  均**听不到声音**；页面无任何报错。
- 用 puppeteer（Chrome）做"点击 → HTTP 200 → `Audio.play()` resolve"验证却**全部通过**。
  这是一个典型的"测试环境与实际环境不一致"陷阱。

## 二、排查过程（按时间顺序）

1. **第一层（已排除）**：音频文件本身。`verify-audio.mjs` 静态校验：5 个包 474 个 m4a 全部存在、非 0 字节。
2. **第二层（已排除）**：前端路径。`app.js` 曾用相对路径 `audio/xxx.m4a`（相对 `/app/` 解析 → 404），
   已修复为 `${BASE}/${pack.id}/audio/xxx.m4a`；`serve.mjs` 已改 `Cache-Control: no-store` 防旧缓存。
3. **第三层（根因，本次修复）**：**服务器媒体响应头不兼容 Safari/WebKit 媒体栈**。
   - 旧 `serve.mjs`：`res.writeHead(200, ...)` 后 `res.end(readFileSync(...))`。
     Node 在 writeHead 已发出头部的情况下不会补 `Content-Length`，实际响应是
     `Transfer-Encoding: chunked` 且**无 Content-Length、无 Accept-Ranges、无视 Range 请求**（Range 也回 200 全文）。
   - Chrome 容忍这种响应（能播），所以 puppeteer 验证全过；**Safari/AppleCoreMedia 强制要求字节区间支持**。
   - 证据链（已用 AVFoundation 实测，AppleCoreMedia 即 Safari 媒体引擎的 HTTP UA）：
     ```
     GET 206 /data/packs/a1-leccion-1/audio/l1-0.m4a range=bytes=0-1    2B     (AppleCoreMedia/1.0.0.25F71)
     GET 206 /data/packs/a1-leccion-1/audio/l1-0.m4a range=bytes=0-11024 11025B (AppleCoreMedia/1.0.0.25F71)
     ```
     AppleCoreMedia 第一步发 `Range: bytes=0-1` 探测；旧服务器对这两个请求都回 200 chunked 全文 → 引擎拒播。
     修复后 AVFoundation 加载测试：`duration=1.536s playable=true audioTracks=1` ✓。
4. 用户录屏（`~/Desktop/DSH/录屏2026-08-15 13.50.59.mov`、`13.51.25.mov`）经 Kimi K2.7 逐帧分析：
   用户在词汇卡例句喇叭、听辨/跟读/听写各播放点点击，画面无任何播放反馈（应用本就没有进度条/波形，
   无视觉反馈属正常设计；"无声"以用户主观听感为准）。

## 三、修复内容（commit 见 git log）

| 文件 | 改动 |
|---|---|
| `scripts/serve.mjs` | v2：显式 `Content-Length`、`Accept-Ranges: bytes`、单区间 `Range` → `206` + `Content-Range`（含后缀区间 `bytes=-500`、越界 `416`）、**请求日志**（时间/方法/状态/URL/Range/字节/UA 前 40 字符） |
| `scripts/verify-audio.mjs` | `--http` 校验升级：GET 必须带 `Content-Length`、`Accept-Ranges: bytes`；`Range: bytes=0-1023` 必须回 `206` + `Content-Range`。**任何服务器改动后必须跑** |
| `app/app.js` | `playAudio` 失败不再静默：页面底部 toast 提示（🔇 播放失败 + 文件名 + 原因），console.error 留痕 |
| `app/index.html` / `app/app.css` | 新增 `#playToast` 元素与样式 |
| `scripts/extract-frames.swift` | 新增：macOS AVFoundation 抽帧工具（无 ffmpeg 依赖），录屏/视频 → PNG 帧，供 Kimi 视觉分析 |

## 四、如何复验（任何一步失败即问题未解决）

```bash
# 1) 服务器必须带新头部（要求：脚本已在跑）
node scripts/verify-audio.mjs --http          # 全绿 = 文件 + Content-Length + Range/206 全过

# 2) 真实浏览器播放（Chrome 全过是必要不充分条件！Chrome 会容忍错误头部）
node scripts/verify-playback.mjs

# 3) Safari 同款媒体引擎实测（最接近真机，无需浏览器）
swift /tmp/avtest.swift    # 输出 playable=true 才算过；URL 换成任意播放点音频

# 4) 人工：真机 Safari/Chrome 硬刷新（Cmd+Shift+R）后点任意播放点，应听到声音；
#    若失败，页面底部会出现 🔇 toast，服务器终端会打出该请求的完整日志。
```

## 五、若仍失败（接力清单，按序执行）

1. **看服务器日志**（serve.mjs 每次请求都打印）：用户点击后有没有对应 URL 的请求？
   - 无请求 → 前端 JS 没触发（浏览器还在用旧 app.js：确认硬刷新；或 F12 Console 看报错）。
   - 有 404 → 路径/包 id 问题（pack.json 引用 vs 磁盘文件名）。
   - 有 200/206 → 音频文件本身或客户端（下一步）。
2. **问用户浏览器与设备**：Safari/Chrome/Edge？iPhone 还是 Mac？iPhone Safari 有额外限制：
   - 静音开关、专注模式、"无痕"标签自动播放策略；
   - iOS 上 `new Audio().play()` 必须在用户手势事件内同步调用（app.js 目前是点击处理器内调用，合规）。
3. **校验 m4a 文件**：`afinfo 文件.m4a` 看格式（edge-tts mp3 → afconvert AAC/m4a，`audio/mp4` MIME 正确）；
   另存一份本地双击播放，排除文件本身损坏。
4. **换引擎对照**：`node scripts/generate-audio.mjs a1-leccion-1 --force --engine=say` 再试（macOS say 生成 aiff→m4a），
   排除 edge-tts 产物的兼容性问题。
5. 仍未解决：把用户浏览器 F12 → Network 面板里该音频请求的**完整响应头**截图发我/Codex，
   与 `serve.mjs` 输出的头部逐项对比。

## 六、历史根因备忘（防重复踩坑）

- **旧缓存 app.js**：曾因相对路径 404 无声；`no-store` 已防，但已打开的页面不刷新仍执行旧 JS。
- **chunked 无 Content-Length**：本次根因。**任何静态服务器改版后必须过 §四 第 1、3 步**。
- 校验脚本只是"代理"，真实环境（Safari/iPhone）与测试环境（Chrome headless）行为可不同，
  尽量用 §四 第 3 步的 AVFoundation 直测缩小差距。

## 七、相关插件（DSH 会话内）

- `kimi-7/pkg-7`：多模态自动召唤 Kimi 插件。用户消息含图片时，自动经 pi CLI
  （`kimi-coding/kimi-for-coding`）分析，把结果以文字块注入模型上下文。字节经
  手写 base64（DSH `btoa` 是 UTF-8 语义，不能用于二进制）→ `.kimi-tmp/` 中转文件 →
  `pi` 分析；按 attachmentId 去重缓存。停止方式：DSH 会话里 `cordis_stop(kimi-7)`。
- `verf-6/pkg-6`：`verify_practice` 工具（静态 + 真实播放双层校验），改动音频链路后必须调用。
